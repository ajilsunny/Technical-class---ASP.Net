@model IEnumerable<Studentdetails.Models.Studentinfo>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Studentdept.Deptname)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.studsubject)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.studmark)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Studentdept.Deptname)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.studsubject)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.studmark)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.StudentinfoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.StudentinfoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.StudentinfoId })
        </td>
    </tr>
}

</table>
